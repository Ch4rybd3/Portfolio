NODE_ENV=production
PORT=3000
SESSION_SECRET=change-this-to-a-long-random-string

# false en HTTP local, true en production derrière Cloudflare (HTTPS)
COOKIE_SECURE=false

# Chemins des données persistées (montés en volume Docker)
DATA_DIR=/app/data
UPLOADS_DIR=/app/uploads
